# Base44 development defaults — placeholders so the app boots without credentials.
# Real secrets are delivered via /run/base44/app.env and override these values.

# Supabase (optional — the frontend degrades gracefully when unset)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
